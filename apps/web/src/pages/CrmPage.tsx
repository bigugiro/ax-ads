import { PagePlaceholder } from '../components/PagePlaceholder';

export function CrmPage() {
  return (
    <PagePlaceholder
      titulo="CRM"
      descricao="Pipeline kanban, leads e automações de jornada (Sprint 5)."
    />
  );
}
